import React from 'react';
import Form from './Form';
import Navbar from './Navbar';

const Home = () => {
    return (
        <div>
            <Navbar></Navbar>
            <Form></Form>
        </div>
    );
};

export default Home;